# FaceAttend AI

Ready-to-open VS Code project for a face recognition attendance prototype.

## Steps

1. Open this folder in VS Code.
2. Run `npm install`
3. Add the face-api.js model files into `public/models`
4. Run `npm run dev`

## Notes

- Data is stored in browser localStorage.
- Recognition will not work until the required model files are added.
